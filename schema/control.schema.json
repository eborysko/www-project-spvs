{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://owasp.org/spvs/schemas/control/v1.json",
  "title": "SPVS Control",
  "type": "object",
  "required": ["id", "category", "sub_category", "description", "level", "mappings", "metadata"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "pattern": "^V[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "category": {
      "type": "object",
      "required": ["id", "name"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^V[0-9]+$" },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "sub_category": {
      "type": "object",
      "required": ["id", "name"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^V[0-9]+\\.[0-9]+$" },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "description": { "type": "string" },
    "level": { "type": "integer", "enum": [1, 2, 3] },
    "mappings": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["items"],
        "additionalProperties": false,
        "properties": {
          "items": {
            "type": "array",
            "minItems": 1,
            "items": {
              "oneOf": [
                { "type": "string", "minLength": 1 },
                {
                  "type": "object",
                  "required": ["id"],
                  "additionalProperties": false,
                  "properties": {
                    "id": { "type": "string", "minLength": 1 },
                    "description": { "type": "string" },
                    "note": { "type": "string" }
                  }
                }
              ]
            }
          }
        }
      }
    },
    "metadata": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": false,
      "properties": {
        "status": { "type": "string", "enum": ["active", "deprecated", "moved", "deleted", "deleted_merged_to"] },
        "change_tags": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["type"],
            "additionalProperties": false,
            "properties": {
              "type": { "type": "string", "enum": ["ADDED", "MODIFIED", "MOVED_FROM", "MOVED_TO", "DELETED", "DELETED_MERGED_TO", "LEVEL_CHANGE", "SPLIT_FROM"] },
              "reference": { "type": "string" },
              "from_level": { "type": "integer", "enum": [1, 2, 3] },
              "to_level": { "type": "integer", "enum": [1, 2, 3] }
            }
          }
        },
        "introduced_in": { "type": "string" },
        "last_modified_in": { "type": "string" },
        "moved_to": { "type": "string" },
        "owner": { "type": "string" }
      }
    }
  },
  "allOf": [
    {
      "$comment": "Active controls must have non-empty description and at least one framework mapping. Tombstones (status moved/deleted/etc.) may have empty content.",
      "if": {
        "type": "object",
        "required": ["metadata"],
        "properties": {
          "metadata": {
            "type": "object",
            "required": ["status"],
            "properties": {
              "status": { "const": "active" }
            }
          }
        }
      },
      "then": {
        "properties": {
          "description": { "minLength": 1 },
          "mappings": { "minProperties": 1 }
        }
      }
    }
  ]
}
